---
---
<div class="project-card">
  <h4 class="font-accent">GameSim in C++</h4>
  <p>A C++ terminal application simulating a game with characters, items, and combat, demonstrating object-oriented programming principles.</p>
  <div class="tags">
    <span class="tag">C++</span>
    <span class="tag">OOP</span>
    <span class="tag">GameDev</span>
  </div>
  <a href="#" target="_blank" rel="noopener noreferrer" class="btn">
    View Project
  </a>
</div>

<style>
.project-card {
  display: flex;
  flex-direction: column;
  padding: calc(var(--space-unit) * 1.5);
  transition: all var(--transition-fast);
  border-bottom: 1px solid var(--color-border);
  flex-basis: 50%;
}

.project-card:hover {
  transform: translateY(-4px);
  border-color: var(--color-accent-tertiary);
}

.project-card h4 {
  margin-bottom: var(--space-unit);
  color: var(--color-accent-tertiary);
}

.project-card p {
  flex-grow: 1;
  color: var(--color-text-secondary);
}

.tags {
  margin: var(--space-unit) 0;
  display: flex;
  flex-wrap: wrap;
  gap: calc(var(--space-unit) / 2);
}

.tag {
  background-color: #2c2c2c;
  color: #a3a3a3;
  padding: calc(var(--space-unit) / 4) calc(var(--space-unit) / 2);
  border-radius: var(--radius-sm);
  font-size: 0.75rem;
  font-family: var(--font-primary);
}

.btn {
  margin-top: auto;
  text-align: center;
  border-color: var(--color-accent-tertiary);
  color: var(--color-accent-tertiary);
}

.btn:hover {
    background-color: var(--color-accent-tertiary);
    color: var(--color-background);
}
</style>